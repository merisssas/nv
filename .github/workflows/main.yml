name: Replace module path mampirmasss/B -> merisssas/nv

on:
  workflow_dispatch:
  push:
    branches: ["main"]

permissions:
  contents: write

concurrency:
  group: replace-module-path
  cancel-in-progress: false

jobs:
  replace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure ripgrep
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v rg >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y ripgrep
          fi

      - name: Replace occurrences (excluding .github/workflows)
        shell: bash
        run: |
          set -euo pipefail

          FROM='github.com/mampirmasss/B'
          TO='github.com/merisssas/nv'

          echo "Searching occurrences of: $FROM"

          # Cari file yang mengandung FROM, tapi JANGAN sentuh .github/workflows/**
          rg -l -0 --hidden --no-mmap \
            --glob '!.git/**' \
            --glob '!.github/workflows/**' \
            --glob '!vendor/**' \
            --glob '!node_modules/**' \
            "$FROM" . > /tmp/matches || true

          if [ ! -s /tmp/matches ]; then
            echo "No matches found. Nothing to do."
            exit 0
          fi

          echo "Replacing in files:"
          tr '\0' '\n' < /tmp/matches

          # delimiter '|' aman terhadap '/'
          xargs -0 -a /tmp/matches perl -0777 -i -pe "s|\Q$FROM\E|$TO|g"

      - name: Show diff
        shell: bash
        run: |
          set -euo pipefail
          git status --porcelain
          git --no-pager diff

      - name: Commit & push if changed
        shell: bash
        run: |
          set -euo pipefail

          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A
          git commit -m "chore: replace module path mampirmasss/B -> merisssas/nv"
          git push
